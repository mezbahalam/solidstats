<div id="audit-details" class="details-panel hidden">
  <div class="audit-summary">
    <div class="audit-summary-header">
      <h3>Security Audit Summary</h3>
      <span class="audit-date">Last updated: <%= Time.now.strftime("%B %d, %Y at %H:%M") %></span>
    </div>
    <div class="audit-stats">
      <div class="audit-stat">
        <span class="stat-value"><%= @audit_output.scan(/CVE-\d+-\d+/).uniq.size %></span>
        <span class="stat-label">CVEs</span>
      </div>
      <div class="audit-stat">
        <span class="stat-value"><%= @audit_output.scan(/Name: ([^\n]+)/).uniq.size %></span>
        <span class="stat-label">Affected Gems</span>
      </div>
      <div class="audit-stat">
        <span class="stat-value"><%= @audit_output.scan(/Criticality: (High|Critical)/).size %></span>
        <span class="stat-label">High Severity</span>
      </div>
    </div>
  </div>
  
  <% if @audit_output.include?('No vulnerabilities found') %>
    <div class="no-vulnerabilities">
      <p>✅ No vulnerabilities found in your dependencies.</p>
    </div>
  <% else %>
    <div class="audit-filters">
      <button class="filter-btn active" data-filter="all">All</button>
      <button class="filter-btn" data-filter="high">High Severity</button>
      <button class="filter-btn" data-filter="copy-solutions">Copy All Solutions</button>
    </div>

    <div class="vulnerabilities-list">
      <% @audit_output.scan(/Name: (.*?)(?=Name: |\z)/m).each do |vulnerability_block| %>
        <% vulnerability = vulnerability_block[0] %>
        <% 
          gem_name = vulnerability.match(/Name: ([^\n]+)/)&.captures&.first
          version = vulnerability.match(/Version: ([^\n]+)/)&.captures&.first
          cve = vulnerability.match(/CVE: ([^\n]+)/)&.captures&.first
          criticality = vulnerability.match(/Criticality: ([^\n]+)/)&.captures&.first
          title = vulnerability.match(/Title: ([^\n]+)/)&.captures&.first
          solution = vulnerability.match(/Solution: ([^\n]+)/)&.captures&.first
          is_high = criticality == "High" || criticality == "Critical"
        %>

        <div class="vulnerability-card <%= is_high ? 'high-severity' : '' %>">
          <div class="vulnerability-header">
            <h4><%= gem_name %> <span class="version"><%= version %></span></h4>
            <span class="severity <%= criticality&.downcase %>"><%= criticality %></span>
          </div>
          <div class="vulnerability-body">
            <p class="title"><%= title %></p>
            <% if cve %>
              <p class="cve"><span>CVE:</span> <%= cve %></p>
            <% end %>
            <div class="solution">
              <div class="solution-header">
                <h5>Solution:</h5>
                <button class="copy-btn" data-solution="<%= gem_name %>@<%= solution %>">
                  <span class="copy-icon">📋</span> Copy
                </button>
              </div>
              <code><%= gem_name %>@<%= solution %></code>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>

<style>
  /* Additional CSS for audit details */
  .audit-summary {
    background: #f8f9fa;
    border-radius: 8px;
    margin-bottom: 1.5rem;
    padding: 1rem;
  }
  
  .audit-summary-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }
  
  .audit-summary-header h3 {
    margin: 0;
  }
  
  .audit-date {
    color: #666;
    font-size: 0.85rem;
  }
  
  .audit-stats {
    display: flex;
    justify-content: space-around;
    text-align: center;
  }
  
  .audit-stat {
    display: flex;
    flex-direction: column;
  }
  
  .stat-value {
    font-size: 1.8rem;
    font-weight: 600;
    color: #333;
  }
  
  .stat-label {
    font-size: 0.9rem;
    color: #666;
  }
  
  .audit-filters {
    margin-bottom: 1rem;
    display: flex;
    gap: 0.5rem;
  }
  
  .filter-btn {
    background: #e9ecef;
    border: none;
    border-radius: 4px;
    padding: 0.5rem 1rem;
    cursor: pointer;
    font-size: 0.9rem;
    transition: all 0.2s;
  }
  
  .filter-btn:hover {
    background: #dee2e6;
  }
  
  .filter-btn.active {
    background: #007bff;
    color: white;
  }
  
  .vulnerabilities-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 1rem;
  }
  
  .vulnerability-card {
    border: 1px solid #dee2e6;
    border-radius: 8px;
    overflow: hidden;
  }
  
  .vulnerability-card.high-severity {
    border-left: 4px solid #dc3545;
  }
  
  .vulnerability-header {
    background: #f8f9fa;
    padding: 0.75rem 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .vulnerability-header h4 {
    margin: 0;
    font-size: 1.1rem;
  }
  
  .version {
    color: #6c757d;
    font-size: 0.9rem;
    font-weight: normal;
    margin-left: 0.5rem;
  }
  
  .severity {
    font-size: 0.8rem;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    text-transform: uppercase;
    font-weight: 600;
  }
  
  .severity.high, .severity.critical {
    background: #f8d7da;
    color: #721c24;
  }
  
  .severity.medium {
    background: #fff3cd;
    color: #856404;
  }
  
  .severity.low, .severity.unknown {
    background: #e2e3e5;
    color: #383d41;
  }
  
  .vulnerability-body {
    padding: 1rem;
  }
  
  .title {
    font-weight: 500;
    margin-bottom: 0.8rem;
  }
  
  .cve span {
    font-weight: 600;
  }
  
  .solution {
    margin-top: 1rem;
    background: #f8f9fa;
    padding: 0.8rem;
    border-radius: 4px;
  }
  
  .solution-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
  }
  
  .solution-header h5 {
    margin: 0;
  }
  
  .copy-btn {
    background: #e9ecef;
    border: none;
    border-radius: 4px;
    padding: 0.3rem 0.6rem;
    font-size: 0.8rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.3rem;
    transition: all 0.2s;
  }
  
  .copy-btn:hover {
    background: #dee2e6;
  }
  
  .copy-btn.copied {
    background: #28a745;
    color: white;
  }
  
  .copy-icon {
    font-size: 0.9rem;
  }
  
  .solution code {
    display: block;
    font-family: monospace;
    background: #fff;
    padding: 0.5rem;
    border-radius: 4px;
    border: 1px solid #dee2e6;
  }
  
  .no-vulnerabilities {
    padding: 2rem;
    text-align: center;
    background: #e3f9e5;
    border-radius: 8px;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Copy functionality for solution buttons
    document.querySelectorAll('.copy-btn').forEach(function(button) {
      button.addEventListener('click', function() {
        const solution = this.getAttribute('data-solution');
        navigator.clipboard.writeText(solution).then(() => {
          // Change button appearance temporarily
          const originalText = this.innerHTML;
          this.innerHTML = '<span class="copy-icon">✓</span> Copied!';
          this.classList.add('copied');
          
          setTimeout(() => {
            this.innerHTML = originalText;
            this.classList.remove('copied');
          }, 2000);
        });
      });
    });
    
    // Filter functionality
    document.querySelectorAll('.filter-btn').forEach(function(button) {
      button.addEventListener('click', function() {
        const filter = this.getAttribute('data-filter');
        
        // Update active button
        document.querySelectorAll('.filter-btn').forEach(btn => btn.classList.remove('active'));
        this.classList.add('active');
        
        // Handle special case for copy-solutions
        if (filter === 'copy-solutions') {
          const solutions = [];
          document.querySelectorAll('.vulnerability-card .solution code').forEach(function(code) {
            solutions.push(code.textContent);
          });
          
          // Copy all solutions to clipboard
          navigator.clipboard.writeText(solutions.join('\n')).then(() => {
            alert('All solutions copied to clipboard!');
          });
          
          // Reset to showing all
          document.querySelectorAll('.vulnerability-card').forEach(card => {
            card.style.display = 'block';
          });
          return;
        }
        
        // Filter the cards
        document.querySelectorAll('.vulnerability-card').forEach(function(card) {
          if (filter === 'all') {
            card.style.display = 'block';
          } else if (filter === 'high') {
            card.style.display = card.classList.contains('high-severity') ? 'block' : 'none';
          }
        });
      });
    });
  });
</script>